diff --git a/src/features/training-sessions/documents-tab.module.css b/src/features/training-sessions/documents-tab.module.css
new file mode 100644
index 0000000..90eda9d
--- /dev/null
+++ b/src/features/training-sessions/documents-tab.module.css
@@ -0,0 +1,120 @@
+.locked {
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  justify-content: center;
+  gap: 0.75rem;
+  padding: 3rem 1rem;
+  text-align: center;
+  flex: 1;
+}
+
+.lockedIcon { color: var(--text-muted); }
+
+.lockedTitle {
+  font-size: 1rem;
+  font-weight: 600;
+  color: var(--text);
+}
+
+.lockedDesc {
+  font-size: 0.875rem;
+  color: var(--text-muted);
+  max-width: 24rem;
+  line-height: 1.6;
+}
+
+.wrapper {
+  display: flex;
+  flex-direction: column;
+  height: 100%;
+  gap: 1rem;
+}
+
+.toolbar {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 0.75rem;
+}
+
+.summary {
+  font-size: 0.875rem;
+  color: var(--text-muted);
+}
+
+.tableWrapper {
+  flex: 1;
+  overflow-y: auto;
+  border: 1px solid var(--border);
+  border-radius: 8px;
+}
+
+.table {
+  width: 100%;
+  border-collapse: collapse;
+  font-size: 0.875rem;
+}
+
+.headRow {
+  background-color: var(--charcoal);
+  position: sticky;
+  top: 0;
+}
+
+.th {
+  padding: 0.625rem 0.875rem;
+  text-align: left;
+  font-size: 0.8125rem;
+  font-weight: 600;
+  color: rgba(255, 255, 255, 0.85);
+  white-space: nowrap;
+}
+
+.th:first-child { border-radius: 8px 0 0 0; }
+.th:last-child  { border-radius: 0 8px 0 0; }
+
+.row {
+  border-bottom: 1px solid var(--border);
+  transition: background-color 0.1s;
+}
+
+.row:last-child { border-bottom: none; }
+.row:hover { background-color: var(--bg); }
+
+.td {
+  padding: 0.625rem 0.875rem;
+  color: var(--text);
+  white-space: nowrap;
+}
+
+.center { text-align: center; }
+
+.scoreOk   { color: #16A34A; font-weight: 600; }
+.scoreFail { color: #DC2626; font-weight: 600; }
+.scoreDash { color: var(--text-muted); }
+
+.certGenerated { color: #16A34A; font-weight: 600; font-size: 0.8125rem; }
+.certPending   { color: var(--text-muted); font-size: 0.8125rem; }
+
+.rowActions {
+  display: flex;
+  justify-content: flex-end;
+  gap: 0.375rem;
+}
+
+.actionBtn {
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  width: 1.75rem;
+  height: 1.75rem;
+  border: none;
+  border-radius: 4px;
+  cursor: pointer;
+  background-color: transparent;
+  color: var(--teal);
+  transition: background-color 0.15s;
+}
+
+.actionBtn:hover { background-color: var(--teal-soft); }
diff --git a/src/features/training-sessions/documents-tab.tsx b/src/features/training-sessions/documents-tab.tsx
new file mode 100644
index 0000000..b9c5f55
--- /dev/null
+++ b/src/features/training-sessions/documents-tab.tsx
@@ -0,0 +1,93 @@
+import { FileText, Send } from 'lucide-react'
+import { Button } from '@/components/ui/button/button'
+import type { TrainingSession } from './types'
+import styles from './documents-tab.module.css'
+
+interface DocumentsTabProps {
+  session: TrainingSession
+}
+
+export function DocumentsTab({ session }: DocumentsTabProps) {
+  if (session.status !== 'CONCLUIDO') {
+    return (
+      <div className={styles.locked}>
+        <FileText size={48} strokeWidth={1} className={styles.lockedIcon} />
+        <p className={styles.lockedTitle}>Documentação indisponível</p>
+        <p className={styles.lockedDesc}>
+          Conclua o treinamento na aba &quot;Participantes&quot; para habilitar a geração de certificados e laudos.
+        </p>
+      </div>
+    )
+  }
+
+  const allGenerated = session.participants.every((p) => p.certificateGenerated)
+
+  return (
+    <div className={styles.wrapper}>
+      <div className={styles.toolbar}>
+        <p className={styles.summary}>
+          {session.participants.filter((p) => p.certificateGenerated).length} / {session.participants.length} certificados gerados
+        </p>
+        {!allGenerated && (
+          <Button variant="primary" size="sm" onClick={() => console.log('Gerar todos')}>
+            <FileText size={14} /> Gerar todos
+          </Button>
+        )}
+      </div>
+
+      <div className={styles.tableWrapper}>
+        <table className={styles.table}>
+          <thead>
+            <tr className={styles.headRow}>
+              <th className={styles.th}>Nome</th>
+              <th className={`${styles.th} ${styles.center}`}>Tipo</th>
+              <th className={`${styles.th} ${styles.center}`}>Score</th>
+              <th className={`${styles.th} ${styles.center}`}>Certificado</th>
+              <th className={styles.th} />
+            </tr>
+          </thead>
+          <tbody>
+            {session.participants.map((p) => (
+              <tr key={p.id} className={styles.row}>
+                <td className={styles.td}>{p.name}</td>
+                <td className={`${styles.td} ${styles.center}`}>
+                  {p.type === 'SOMENTE_TEORICA' ? 'Só Teoria' : 'Teoria + Prática'}
+                </td>
+                <td className={`${styles.td} ${styles.center}`}>
+                  {p.score !== null ? (
+                    <span className={p.score >= 70 ? styles.scoreOk : styles.scoreFail}>
+                      {p.score}%
+                    </span>
+                  ) : (
+                    <span className={styles.scoreDash}>—</span>
+                  )}
+                </td>
+                <td className={`${styles.td} ${styles.center}`}>
+                  {p.certificateGenerated ? (
+                    <span className={styles.certGenerated}>Gerado</span>
+                  ) : (
+                    <span className={styles.certPending}>Pendente</span>
+                  )}
+                </td>
+                <td className={styles.td}>
+                  <div className={styles.rowActions}>
+                    {!p.certificateGenerated && (
+                      <button className={styles.actionBtn} onClick={() => console.log('Gerar:', p.id)} title="Gerar certificado">
+                        <FileText size={14} />
+                      </button>
+                    )}
+                    {p.certificateGenerated && (
+                      <button className={styles.actionBtn} onClick={() => console.log('Enviar:', p.id)} title="Enviar por e-mail">
+                        <Send size={14} />
+                      </button>
+                    )}
+                  </div>
+                </td>
+              </tr>
+            ))}
+          </tbody>
+        </table>
+      </div>
+    </div>
+  )
+}
diff --git a/src/features/training-sessions/info-tab.module.css b/src/features/training-sessions/info-tab.module.css
new file mode 100644
index 0000000..5e83fb5
--- /dev/null
+++ b/src/features/training-sessions/info-tab.module.css
@@ -0,0 +1,96 @@
+.wrapper {
+  display: flex;
+  flex-direction: column;
+  height: 100%;
+  gap: 1.5rem;
+}
+
+.content {
+  flex: 1;
+  display: flex;
+  flex-direction: column;
+  gap: 1.5rem;
+}
+
+.companyHeader {
+  display: flex;
+  align-items: center;
+  gap: 1rem;
+  padding: 1rem;
+  background-color: var(--bg);
+  border-radius: 8px;
+  border: 1px solid var(--border);
+}
+
+.logoFallback {
+  width: 3rem;
+  height: 3rem;
+  border-radius: 50%;
+  background-color: var(--teal-soft);
+  color: var(--teal-dark);
+  font-size: 0.875rem;
+  font-weight: 700;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  flex-shrink: 0;
+  border: 1px solid var(--border);
+}
+
+.companyName {
+  font-size: 0.9375rem;
+  font-weight: 600;
+  color: var(--text);
+}
+
+.courseName {
+  font-size: 0.875rem;
+  color: var(--text-2);
+  margin-top: 0.125rem;
+}
+
+.infoGrid {
+  display: flex;
+  flex-direction: column;
+  gap: 0;
+  border: 1px solid var(--border);
+  border-radius: 8px;
+  overflow: hidden;
+}
+
+.row {
+  display: flex;
+  align-items: flex-start;
+  padding: 0.75rem 1rem;
+  border-bottom: 1px solid var(--border);
+  gap: 1rem;
+}
+
+.row:last-child {
+  border-bottom: none;
+}
+
+.rowLabel {
+  font-size: 0.8125rem;
+  font-weight: 500;
+  color: var(--text-muted);
+  min-width: 11rem;
+  flex-shrink: 0;
+}
+
+.rowValue {
+  font-size: 0.875rem;
+  color: var(--text);
+  flex: 1;
+}
+
+.actions {
+  padding-top: 1rem;
+  border-top: 1px solid var(--border);
+  flex-shrink: 0;
+}
+
+.actionsLeft {
+  display: flex;
+  gap: 0.5rem;
+}
diff --git a/src/features/training-sessions/info-tab.tsx b/src/features/training-sessions/info-tab.tsx
new file mode 100644
index 0000000..87822bf
--- /dev/null
+++ b/src/features/training-sessions/info-tab.tsx
@@ -0,0 +1,85 @@
+import { Archive, XCircle } from 'lucide-react'
+import { Button } from '@/components/ui/button/button'
+import { TrainingSessionStatusBadge } from './training-session-status-badge'
+import type { TrainingSession } from './types'
+import styles from './info-tab.module.css'
+
+interface InfoTabProps {
+  session: TrainingSession
+  onArchive: () => void
+  onCancel: () => void
+}
+
+function formatDate(dateStr: string): string {
+  const [year, month, day] = dateStr.split('-')
+  return `${day}/${month}/${year}`
+}
+
+function Row({ label, children }: { label: string; children: React.ReactNode }) {
+  return (
+    <div className={styles.row}>
+      <span className={styles.rowLabel}>{label}</span>
+      <span className={styles.rowValue}>{children}</span>
+    </div>
+  )
+}
+
+export function InfoTab({ session, onArchive, onCancel }: InfoTabProps) {
+  const canArchive = session.status !== 'ARQUIVADO' && session.status !== 'CANCELADO'
+  const canCancel = session.status === 'PLANEJADO' || session.status === 'EM_ANDAMENTO'
+
+  return (
+    <div className={styles.wrapper}>
+      <div className={styles.content}>
+
+        <div className={styles.companyHeader}>
+          <div className={styles.logoFallback}>
+            {session.company.name.split(' ').slice(0, 2).map((w) => w[0]).join('').toUpperCase()}
+          </div>
+          <div>
+            <p className={styles.companyName}>{session.company.name}</p>
+            <p className={styles.courseName}>{session.course.name}</p>
+          </div>
+        </div>
+
+        <div className={styles.infoGrid}>
+          <Row label="Status"><TrainingSessionStatusBadge status={session.status} /></Row>
+          <Row label="Data">{formatDate(session.date)}</Row>
+          <Row label="Local">{session.city} / {session.state}</Row>
+          <Row label="Carga horária">
+            {session.course.theoryHours}h teórica
+            {session.course.practiceHours > 0 && ` + ${session.course.practiceHours}h prática`}
+          </Row>
+          <Row label="Nº estimado">{session.participantCount} participantes</Row>
+          <Row label="Consultor responsável">{session.responsibleConsultant.name}</Row>
+          {session.additionalConsultants.length > 0 && (
+            <Row label="Consultores adicionais">
+              {session.additionalConsultants.map((c) => c.name).join(', ')}
+            </Row>
+          )}
+          {session.notes && (
+            <Row label="Observações">{session.notes}</Row>
+          )}
+        </div>
+
+      </div>
+
+      {(canArchive || canCancel) && (
+        <div className={styles.actions}>
+          <div className={styles.actionsLeft}>
+            {canArchive && (
+              <Button variant="ghost" size="sm" onClick={onArchive}>
+                <Archive size={14} /> Arquivar
+              </Button>
+            )}
+            {canCancel && (
+              <Button variant="danger" size="sm" onClick={onCancel}>
+                <XCircle size={14} /> Cancelar Treinamento
+              </Button>
+            )}
+          </div>
+        </div>
+      )}
+    </div>
+  )
+}
diff --git a/src/features/training-sessions/participants-tab.module.css b/src/features/training-sessions/participants-tab.module.css
new file mode 100644
index 0000000..db61e16
--- /dev/null
+++ b/src/features/training-sessions/participants-tab.module.css
@@ -0,0 +1,186 @@
+.wrapper {
+  display: flex;
+  flex-direction: column;
+  height: 100%;
+  gap: 1rem;
+}
+
+.toolbar {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 0.75rem;
+}
+
+.count {
+  font-size: 0.875rem;
+  color: var(--text-muted);
+}
+
+.toolbarActions {
+  display: flex;
+  gap: 0.5rem;
+}
+
+.addForm {
+  border: 1px solid var(--border);
+  border-radius: 8px;
+  padding: 1rem;
+  background-color: var(--bg);
+  display: flex;
+  flex-direction: column;
+  gap: 0.75rem;
+}
+
+.addFormTitle {
+  font-size: 0.875rem;
+  font-weight: 600;
+  color: var(--text);
+}
+
+.addFormGrid {
+  display: grid;
+  grid-template-columns: 1fr 1fr;
+  gap: 0.75rem;
+}
+
+.field {
+  display: flex;
+  flex-direction: column;
+  gap: 0.25rem;
+}
+
+.label {
+  font-size: 0.8125rem;
+  font-weight: 500;
+  color: var(--text);
+}
+
+.input {
+  height: 2rem;
+  padding: 0 0.625rem;
+  border: 1px solid var(--border);
+  border-radius: 4px;
+  font-size: 0.8125rem;
+  color: var(--text);
+  background-color: var(--surface);
+  outline: none;
+  width: 100%;
+}
+
+.input:focus {
+  border-color: var(--teal);
+  box-shadow: 0 0 0 0.125rem rgba(56, 190, 200, 0.15);
+}
+
+.addFormActions {
+  display: flex;
+  justify-content: flex-end;
+  gap: 0.5rem;
+}
+
+.tableWrapper {
+  flex: 1;
+  overflow-y: auto;
+  border: 1px solid var(--border);
+  border-radius: 8px;
+}
+
+.table {
+  width: 100%;
+  border-collapse: collapse;
+  font-size: 0.875rem;
+}
+
+.headRow {
+  background-color: var(--charcoal);
+  position: sticky;
+  top: 0;
+}
+
+.th {
+  padding: 0.625rem 0.875rem;
+  text-align: left;
+  font-size: 0.8125rem;
+  font-weight: 600;
+  color: rgba(255, 255, 255, 0.85);
+  white-space: nowrap;
+}
+
+.th:first-child { border-radius: 8px 0 0 0; }
+.th:last-child  { border-radius: 0 8px 0 0; }
+
+.row {
+  border-bottom: 1px solid var(--border);
+  transition: background-color 0.1s;
+}
+
+.row:last-child { border-bottom: none; }
+.row:hover { background-color: var(--bg); }
+
+.td {
+  padding: 0.625rem 0.875rem;
+  color: var(--text);
+  max-width: 12rem;
+  overflow: hidden;
+  text-overflow: ellipsis;
+  white-space: nowrap;
+}
+
+.center { text-align: center; }
+
+.typeBtn {
+  display: inline-flex;
+  align-items: center;
+  padding: 0.2rem 0.625rem;
+  border-radius: 4px;
+  font-size: 0.75rem;
+  font-weight: 600;
+  border: none;
+  cursor: pointer;
+  transition: opacity 0.15s;
+}
+
+.typeBtn:hover { opacity: 0.8; }
+
+.typeSoloTeoria {
+  background-color: var(--mauve-soft);
+  color: var(--mauve);
+}
+
+.typePratica {
+  background-color: var(--teal-soft);
+  color: var(--teal-dark);
+}
+
+.statusBadge {
+  display: inline-flex;
+  align-items: center;
+  padding: 0.2rem 0.625rem;
+  border-radius: 4px;
+  font-size: 0.75rem;
+  font-weight: 600;
+  white-space: nowrap;
+}
+
+.statusPendente        { background-color: var(--bg); color: var(--text-muted); border: 1px solid var(--border); }
+.statusEmAvaliacao     { background-color: var(--teal-soft); color: var(--teal-dark); }
+.statusAprovado        { background-color: #DCFCE7; color: #16A34A; }
+.statusNecessita       { background-color: #FEE2E2; color: #DC2626; }
+
+.empty {
+  font-size: 0.875rem;
+  color: var(--text-muted);
+  text-align: center;
+  padding: 2rem 0;
+  flex: 1;
+}
+
+.footer {
+  display: flex;
+  justify-content: flex-end;
+  gap: 0.5rem;
+  padding-top: 1rem;
+  border-top: 1px solid var(--border);
+  flex-shrink: 0;
+}
diff --git a/src/features/training-sessions/participants-tab.tsx b/src/features/training-sessions/participants-tab.tsx
new file mode 100644
index 0000000..81e6e9b
--- /dev/null
+++ b/src/features/training-sessions/participants-tab.tsx
@@ -0,0 +1,202 @@
+'use client'
+
+import { useState } from 'react'
+import { UserPlus, Upload, Check } from 'lucide-react'
+import { Button } from '@/components/ui/button/button'
+import type { TrainingSession, TrainingParticipant, ParticipationType, ParticipantStatus } from './types'
+import styles from './participants-tab.module.css'
+
+interface ParticipantsTabProps {
+  session: TrainingSession
+  onUpdateSession: (updated: TrainingSession) => void
+}
+
+const statusLabels: Record<ParticipantStatus, string> = {
+  PENDENTE: 'Pendente',
+  EM_AVALIACAO: 'Em Avaliação',
+  APROVADO: 'Aprovado',
+  NECESSITA_REAVALIACAO: 'Necessita Reavaliação',
+}
+
+const statusColors: Record<ParticipantStatus, string> = {
+  PENDENTE: styles.statusPendente,
+  EM_AVALIACAO: styles.statusEmAvaliacao,
+  APROVADO: styles.statusAprovado,
+  NECESSITA_REAVALIACAO: styles.statusNecessita,
+}
+
+interface AddParticipantForm {
+  name: string
+  cpf: string
+  email: string
+  cnhCategory: string
+  cnhExpiration: string
+}
+
+const emptyForm: AddParticipantForm = {
+  name: '', cpf: '', email: '', cnhCategory: '', cnhExpiration: '',
+}
+
+export function ParticipantsTab({ session, onUpdateSession }: ParticipantsTabProps) {
+  const [showAddForm, setShowAddForm] = useState(false)
+  const [form, setForm] = useState<AddParticipantForm>(emptyForm)
+
+  function setField(field: keyof AddParticipantForm, value: string) {
+    setForm((prev) => ({ ...prev, [field]: value }))
+  }
+
+  function toggleType(participantId: number) {
+    const updated: TrainingSession = {
+      ...session,
+      participants: session.participants.map((p) =>
+        p.id === participantId
+          ? { ...p, type: (p.type === 'SOMENTE_TEORICA' ? 'TEORICA_E_PRATICA' : 'SOMENTE_TEORICA') as ParticipationType }
+          : p
+      ),
+    }
+    onUpdateSession(updated)
+  }
+
+  function addParticipant() {
+    if (!form.name.trim() || !form.cpf.trim()) return
+    const newP: TrainingParticipant = {
+      id: Date.now(),
+      name: form.name,
+      cpf: form.cpf,
+      email: form.email,
+      cnhCategory: form.cnhCategory,
+      cnhExpiration: form.cnhExpiration,
+      type: 'TEORICA_E_PRATICA',
+      status: 'PENDENTE',
+      score: null,
+      certificateGenerated: false,
+    }
+    onUpdateSession({
+      ...session,
+      participants: [...session.participants, newP],
+      participantCount: session.participantCount + 1,
+    })
+    setForm(emptyForm)
+    setShowAddForm(false)
+  }
+
+  function handleStart() {
+    onUpdateSession({ ...session, status: 'EM_ANDAMENTO' })
+  }
+
+  function handleComplete() {
+    onUpdateSession({ ...session, status: 'CONCLUIDO' })
+  }
+
+  const canStart = session.status === 'PLANEJADO'
+  const canComplete =
+    session.status === 'EM_ANDAMENTO' &&
+    session.participants.length > 0 &&
+    session.participants.every(
+      (p) => p.type === 'SOMENTE_TEORICA' || p.status === 'APROVADO' || p.status === 'NECESSITA_REAVALIACAO'
+    )
+
+  return (
+    <div className={styles.wrapper}>
+
+      <div className={styles.toolbar}>
+        <p className={styles.count}>{session.participants.length} participante(s)</p>
+        <div className={styles.toolbarActions}>
+          <Button variant="ghost" size="sm" onClick={() => console.log('Import XLSX')}>
+            <Upload size={14} /> Importar planilha
+          </Button>
+          <Button variant="secondary" size="sm" onClick={() => setShowAddForm((v) => !v)}>
+            <UserPlus size={14} /> Adicionar manualmente
+          </Button>
+        </div>
+      </div>
+
+      {showAddForm && (
+        <div className={styles.addForm}>
+          <p className={styles.addFormTitle}>Novo Participante</p>
+          <div className={styles.addFormGrid}>
+            <div className={styles.field}>
+              <label className={styles.label}>Nome *</label>
+              <input className={styles.input} value={form.name} onChange={(e) => setField('name', e.target.value)} />
+            </div>
+            <div className={styles.field}>
+              <label className={styles.label}>CPF *</label>
+              <input className={styles.input} value={form.cpf} onChange={(e) => setField('cpf', e.target.value)} placeholder="000.000.000-00" />
+            </div>
+            <div className={styles.field}>
+              <label className={styles.label}>E-mail</label>
+              <input className={styles.input} type="email" value={form.email} onChange={(e) => setField('email', e.target.value)} />
+            </div>
+            <div className={styles.field}>
+              <label className={styles.label}>Cat. CNH</label>
+              <input className={styles.input} value={form.cnhCategory} onChange={(e) => setField('cnhCategory', e.target.value)} placeholder="B" />
+            </div>
+            <div className={styles.field}>
+              <label className={styles.label}>Vencimento CNH</label>
+              <input className={styles.input} type="date" value={form.cnhExpiration} onChange={(e) => setField('cnhExpiration', e.target.value)} />
+            </div>
+          </div>
+          <div className={styles.addFormActions}>
+            <Button variant="ghost" size="sm" onClick={() => { setShowAddForm(false); setForm(emptyForm) }}>Cancelar</Button>
+            <Button variant="primary" size="sm" disabled={!form.name.trim() || !form.cpf.trim()} onClick={addParticipant}>
+              <Check size={14} /> Confirmar
+            </Button>
+          </div>
+        </div>
+      )}
+
+      {session.participants.length === 0 ? (
+        <p className={styles.empty}>Nenhum participante inscrito ainda.</p>
+      ) : (
+        <div className={styles.tableWrapper}>
+          <table className={styles.table}>
+            <thead>
+              <tr className={styles.headRow}>
+                <th className={styles.th}>Nome</th>
+                <th className={styles.th}>CPF</th>
+                <th className={`${styles.th} ${styles.center}`}>Tipo</th>
+                <th className={`${styles.th} ${styles.center}`}>Avaliação</th>
+              </tr>
+            </thead>
+            <tbody>
+              {session.participants.map((p) => (
+                <tr key={p.id} className={styles.row}>
+                  <td className={styles.td}>{p.name}</td>
+                  <td className={styles.td}>{p.cpf}</td>
+                  <td className={`${styles.td} ${styles.center}`}>
+                    <button
+                      className={`${styles.typeBtn} ${p.type === 'SOMENTE_TEORICA' ? styles.typeSoloTeoria : styles.typePratica}`}
+                      onClick={() => toggleType(p.id)}
+                      title="Clique para alternar"
+                    >
+                      {p.type === 'SOMENTE_TEORICA' ? 'Só Teoria' : 'Teoria + Prática'}
+                    </button>
+                  </td>
+                  <td className={`${styles.td} ${styles.center}`}>
+                    <span className={`${styles.statusBadge} ${statusColors[p.status]}`}>
+                      {statusLabels[p.status]}
+                    </span>
+                  </td>
+                </tr>
+              ))}
+            </tbody>
+          </table>
+        </div>
+      )}
+
+      <div className={styles.footer}>
+        {canStart && (
+          <Button variant="primary" size="sm" onClick={handleStart}>
+            Iniciar Treinamento
+          </Button>
+        )}
+        {canComplete && (
+          <Button variant="primary" size="sm" onClick={handleComplete}>
+            Concluir Treinamento
+          </Button>
+        )}
+      </div>
+
+    </div>
+  )
+}
diff --git a/src/features/training-sessions/qr-code-tab.module.css b/src/features/training-sessions/qr-code-tab.module.css
new file mode 100644
index 0000000..342dbf4
--- /dev/null
+++ b/src/features/training-sessions/qr-code-tab.module.css
@@ -0,0 +1,58 @@
+.wrapper {
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  gap: 1.5rem;
+  padding-top: 1rem;
+}
+
+.qrPlaceholder {
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  gap: 0.75rem;
+  padding: 2rem;
+  border: 2px dashed var(--border);
+  border-radius: 8px;
+  background-color: var(--bg);
+  width: 100%;
+}
+
+.qrIcon { color: var(--charcoal); }
+
+.qrLabel {
+  font-size: 0.8125rem;
+  color: var(--text-muted);
+  text-align: center;
+}
+
+.linkBox {
+  width: 100%;
+  background-color: var(--bg);
+  border: 1px solid var(--border);
+  border-radius: 8px;
+  padding: 0.875rem 1rem;
+}
+
+.linkLabel {
+  font-size: 0.75rem;
+  color: var(--text-muted);
+  margin-bottom: 0.25rem;
+}
+
+.linkUrl {
+  font-size: 0.875rem;
+  color: var(--teal-dark);
+  font-weight: 500;
+  word-break: break-all;
+}
+
+.actions { display: flex; gap: 0.75rem; }
+
+.hint {
+  font-size: 0.8125rem;
+  color: var(--text-muted);
+  text-align: center;
+  line-height: 1.6;
+  max-width: 28rem;
+}
diff --git a/src/features/training-sessions/qr-code-tab.tsx b/src/features/training-sessions/qr-code-tab.tsx
new file mode 100644
index 0000000..e16b534
--- /dev/null
+++ b/src/features/training-sessions/qr-code-tab.tsx
@@ -0,0 +1,44 @@
+import { ExternalLink, Copy, QrCode } from 'lucide-react'
+import { Button } from '@/components/ui/button/button'
+import type { TrainingSession } from './types'
+import styles from './qr-code-tab.module.css'
+
+interface QrCodeTabProps {
+  session: TrainingSession
+}
+
+export function QrCodeTab({ session }: QrCodeTabProps) {
+  const publicUrl = `simtc.com.br/register/${session.qrCodeToken}`
+
+  function copyLink() {
+    navigator.clipboard.writeText(`https://${publicUrl}`)
+  }
+
+  return (
+    <div className={styles.wrapper}>
+      <div className={styles.qrPlaceholder}>
+        <QrCode size={180} strokeWidth={1} className={styles.qrIcon} />
+        <p className={styles.qrLabel}>QR Code — {session.company.name}</p>
+      </div>
+      <div className={styles.linkBox}>
+        <p className={styles.linkLabel}>Link do formulário público:</p>
+        <p className={styles.linkUrl}>{publicUrl}</p>
+      </div>
+      <div className={styles.actions}>
+        <Button variant="secondary" size="sm" onClick={copyLink}>
+          <Copy size={14} /> Copiar link
+        </Button>
+        <Button
+          variant="primary"
+          size="sm"
+          onClick={() => window.open(`/training-sessions/${session.id}/qr-code`, '_blank')}
+        >
+          <ExternalLink size={14} /> Abrir em tela cheia
+        </Button>
+      </div>
+      <p className={styles.hint}>
+        Projete o QR Code na sala para que os participantes façam o auto-cadastro pelo celular, sem precisar digitar nenhum código de turma.
+      </p>
+    </div>
+  )
+}
diff --git a/src/features/training-sessions/training-session-add-form.module.css b/src/features/training-sessions/training-session-add-form.module.css
new file mode 100644
index 0000000..770e7f5
--- /dev/null
+++ b/src/features/training-sessions/training-session-add-form.module.css
@@ -0,0 +1,137 @@
+.wrapper {
+  display: flex;
+  flex-direction: column;
+  gap: 1.5rem;
+  height: 100%;
+}
+
+.form {
+  display: flex;
+  flex-direction: column;
+  gap: 1rem;
+  flex: 1;
+}
+
+.row2 {
+  display: grid;
+  grid-template-columns: 1fr 1fr;
+  gap: 1rem;
+}
+
+.field {
+  display: flex;
+  flex-direction: column;
+  gap: 0.375rem;
+}
+
+.label {
+  font-size: 0.875rem;
+  font-weight: 500;
+  color: var(--text);
+}
+
+.required {
+  color: #DC2626;
+}
+
+.input {
+  height: 2.25rem;
+  padding: 0 0.75rem;
+  border: 1px solid var(--border);
+  border-radius: 4px;
+  font-size: 0.875rem;
+  color: var(--text);
+  background-color: var(--surface);
+  outline: none;
+  transition: border-color 0.15s, box-shadow 0.15s;
+  width: 100%;
+}
+
+.input:focus {
+  border-color: var(--teal);
+  box-shadow: 0 0 0 0.1875rem rgba(56, 190, 200, 0.15);
+}
+
+.textarea {
+  padding: 0.5rem 0.75rem;
+  border: 1px solid var(--border);
+  border-radius: 4px;
+  font-size: 0.875rem;
+  color: var(--text);
+  background-color: var(--surface);
+  outline: none;
+  resize: vertical;
+  font-family: inherit;
+  transition: border-color 0.15s, box-shadow 0.15s;
+  width: 100%;
+}
+
+.textarea:focus {
+  border-color: var(--teal);
+  box-shadow: 0 0 0 0.1875rem rgba(56, 190, 200, 0.15);
+}
+
+.multiSelect {
+  display: flex;
+  flex-direction: column;
+  gap: 0.375rem;
+  border: 1px solid var(--border);
+  border-radius: 4px;
+  padding: 0.5rem;
+  background-color: var(--surface);
+  max-height: 9rem;
+  overflow-y: auto;
+}
+
+.checkItem {
+  display: flex;
+  align-items: center;
+  gap: 0.5rem;
+  padding: 0.375rem 0.5rem;
+  border-radius: 4px;
+  font-size: 0.875rem;
+  color: var(--text);
+  cursor: pointer;
+  transition: background-color 0.1s;
+}
+
+.checkItem:hover {
+  background-color: var(--bg);
+}
+
+.checkItemSelected {
+  background-color: var(--teal-soft);
+  color: var(--teal-dark);
+}
+
+.checkbox {
+  accent-color: var(--teal);
+  width: 1rem;
+  height: 1rem;
+  flex-shrink: 0;
+}
+
+.noConsultants {
+  font-size: 0.875rem;
+  color: var(--text-muted);
+  text-align: center;
+  padding: 0.5rem 0;
+}
+
+.actions {
+  display: flex;
+  justify-content: space-between;
+  align-items: center;
+  padding-top: 1rem;
+  border-top: 1px solid var(--border);
+  flex-shrink: 0;
+}
+
+.actionsRight {
+  display: flex;
+  gap: 0.5rem;
+}
+
+@media (max-width: 30rem) {
+  .row2 { grid-template-columns: 1fr; }
+}
diff --git a/src/features/training-sessions/training-session-add-form.tsx b/src/features/training-sessions/training-session-add-form.tsx
new file mode 100644
index 0000000..feed441
--- /dev/null
+++ b/src/features/training-sessions/training-session-add-form.tsx
@@ -0,0 +1,203 @@
+'use client'
+
+import { useState } from 'react'
+import { Button } from '@/components/ui/button/button'
+import { Select } from '@/components/ui/select/select'
+import { mockCompanies, mockCourses, mockConsultants } from './mock-data'
+import styles from './training-session-add-form.module.css'
+
+export interface TrainingSessionFormData {
+  companyId: number
+  courseId: number
+  responsibleConsultantId: number
+  additionalConsultantIds: number[]
+  city: string
+  state: string
+  date: string
+  participantCount: string
+  notes: string
+}
+
+const BR_STATES = [
+  'AC','AL','AP','AM','BA','CE','DF','ES','GO','MA','MT','MS','MG','PA',
+  'PB','PR','PE','PI','RJ','RN','RS','RO','RR','SC','SP','SE','TO',
+]
+
+interface TrainingSessionAddFormProps {
+  onSave: (data: TrainingSessionFormData) => void
+  onCancel: () => void
+}
+
+const empty: TrainingSessionFormData = {
+  companyId: 0,
+  courseId: 0,
+  responsibleConsultantId: 0,
+  additionalConsultantIds: [],
+  city: '',
+  state: '',
+  date: '',
+  participantCount: '',
+  notes: '',
+}
+
+export function TrainingSessionAddForm({ onSave, onCancel }: TrainingSessionAddFormProps) {
+  const [form, setForm] = useState<TrainingSessionFormData>(empty)
+
+  function set<K extends keyof TrainingSessionFormData>(field: K, value: TrainingSessionFormData[K]) {
+    setForm((prev) => ({ ...prev, [field]: value }))
+  }
+
+  function toggleAdditional(id: number) {
+    setForm((prev) => {
+      const already = prev.additionalConsultantIds.includes(id)
+      return {
+        ...prev,
+        additionalConsultantIds: already
+          ? prev.additionalConsultantIds.filter((x) => x !== id)
+          : [...prev.additionalConsultantIds, id],
+      }
+    })
+  }
+
+  const availableAdditional = mockConsultants.filter(
+    (c) => c.id !== form.responsibleConsultantId,
+  )
+
+  const isValid =
+    form.companyId > 0 &&
+    form.courseId > 0 &&
+    form.responsibleConsultantId > 0 &&
+    form.city.trim() !== '' &&
+    form.state !== '' &&
+    form.date !== ''
+
+  return (
+    <div className={styles.wrapper}>
+      <div className={styles.form}>
+
+        <div className={styles.field}>
+          <label className={styles.label}>Empresa <span className={styles.required}>*</span></label>
+          <Select
+            value={form.companyId === 0 ? '' : String(form.companyId)}
+            onChange={(v) => set('companyId', Number(v))}
+            options={mockCompanies.map((c) => ({ label: c.name, value: String(c.id) }))}
+            placeholder="Selecionar empresa"
+          />
+        </div>
+
+        <div className={styles.field}>
+          <label className={styles.label}>Curso <span className={styles.required}>*</span></label>
+          <Select
+            value={form.courseId === 0 ? '' : String(form.courseId)}
+            onChange={(v) => set('courseId', Number(v))}
+            options={mockCourses.map((c) => ({ label: c.name, value: String(c.id) }))}
+            placeholder="Selecionar curso"
+          />
+        </div>
+
+        <div className={styles.field}>
+          <label className={styles.label}>Consultor Responsável <span className={styles.required}>*</span></label>
+          <Select
+            value={form.responsibleConsultantId === 0 ? '' : String(form.responsibleConsultantId)}
+            onChange={(v) => {
+              const newId = Number(v)
+              set('responsibleConsultantId', newId)
+              set('additionalConsultantIds', form.additionalConsultantIds.filter((id) => id !== newId))
+            }}
+            options={mockConsultants.map((c) => ({ label: c.name, value: String(c.id) }))}
+            placeholder="Selecionar consultor"
+          />
+        </div>
+
+        <div className={styles.row2}>
+          <div className={styles.field}>
+            <label className={styles.label}>Cidade <span className={styles.required}>*</span></label>
+            <input
+              className={styles.input}
+              value={form.city}
+              onChange={(e) => set('city', e.target.value)}
+              placeholder="Ex: Guarulhos"
+            />
+          </div>
+          <div className={styles.field}>
+            <label className={styles.label}>UF <span className={styles.required}>*</span></label>
+            <Select
+              value={form.state}
+              onChange={(v) => set('state', v)}
+              options={BR_STATES.map((s) => ({ label: s, value: s }))}
+              placeholder="UF"
+            />
+          </div>
+        </div>
+
+        <div className={styles.row2}>
+          <div className={styles.field}>
+            <label className={styles.label}>Data <span className={styles.required}>*</span></label>
+            <input
+              className={styles.input}
+              type="date"
+              value={form.date}
+              onChange={(e) => set('date', e.target.value)}
+            />
+          </div>
+          <div className={styles.field}>
+            <label className={styles.label}>Nº estimado de participantes</label>
+            <input
+              className={styles.input}
+              type="number"
+              min="0"
+              value={form.participantCount}
+              onChange={(e) => set('participantCount', e.target.value)}
+              placeholder="0"
+            />
+          </div>
+        </div>
+
+        <div className={styles.field}>
+          <label className={styles.label}>Consultores Adicionais</label>
+          <div className={styles.multiSelect}>
+            {availableAdditional.length === 0 && (
+              <p className={styles.noConsultants}>Nenhum consultor disponível</p>
+            )}
+            {availableAdditional.map((c) => {
+              const checked = form.additionalConsultantIds.includes(c.id)
+              return (
+                <label key={c.id} className={`${styles.checkItem} ${checked ? styles.checkItemSelected : ''}`}>
+                  <input
+                    type="checkbox"
+                    className={styles.checkbox}
+                    checked={checked}
+                    onChange={() => toggleAdditional(c.id)}
+                  />
+                  {c.name}
+                </label>
+              )
+            })}
+          </div>
+        </div>
+
+        <div className={styles.field}>
+          <label className={styles.label}>Observações</label>
+          <textarea
+            className={styles.textarea}
+            value={form.notes}
+            onChange={(e) => set('notes', e.target.value)}
+            placeholder="Ex: Treinamento começa às 8h"
+            rows={3}
+          />
+        </div>
+
+      </div>
+
+      <div className={styles.actions}>
+        <div />
+        <div className={styles.actionsRight}>
+          <Button variant="secondary" size="sm" onClick={onCancel}>Cancelar</Button>
+          <Button variant="primary" size="sm" disabled={!isValid} onClick={() => onSave(form)}>
+            Salvar
+          </Button>
+        </div>
+      </div>
+    </div>
+  )
+}
diff --git a/src/features/training-sessions/training-session-detail-tabs.module.css b/src/features/training-sessions/training-session-detail-tabs.module.css
new file mode 100644
index 0000000..0443b86
--- /dev/null
+++ b/src/features/training-sessions/training-session-detail-tabs.module.css
@@ -0,0 +1,41 @@
+.wrapper {
+  display: flex;
+  flex-direction: column;
+  height: 100%;
+}
+
+.tabList {
+  display: flex;
+  border-bottom: 2px solid var(--border);
+  flex-shrink: 0;
+  margin-bottom: 1.25rem;
+}
+
+.tab {
+  padding: 0.625rem 1rem;
+  font-size: 0.875rem;
+  font-weight: 500;
+  color: var(--text-muted);
+  background: none;
+  border: none;
+  cursor: pointer;
+  border-bottom: 2px solid transparent;
+  margin-bottom: -2px;
+  transition: color 0.15s, border-color 0.15s;
+  white-space: nowrap;
+}
+
+.tab:hover { color: var(--text); }
+
+.tabActive {
+  color: var(--teal-dark);
+  border-bottom-color: var(--teal);
+  font-weight: 600;
+}
+
+.panel {
+  flex: 1;
+  overflow-y: auto;
+  display: flex;
+  flex-direction: column;
+}
diff --git a/src/features/training-sessions/training-session-detail-tabs.tsx b/src/features/training-sessions/training-session-detail-tabs.tsx
new file mode 100644
index 0000000..190d020
--- /dev/null
+++ b/src/features/training-sessions/training-session-detail-tabs.tsx
@@ -0,0 +1,67 @@
+'use client'
+
+import { useState } from 'react'
+import { InfoTab } from './info-tab'
+import { ParticipantsTab } from './participants-tab'
+import { QrCodeTab } from './qr-code-tab'
+import { DocumentsTab } from './documents-tab'
+import type { TrainingSession } from './types'
+import styles from './training-session-detail-tabs.module.css'
+
+type Tab = 'info' | 'participants' | 'qrcode' | 'documents'
+
+interface TrainingSessionDetailTabsProps {
+  session: TrainingSession
+  onUpdateSession: (updated: TrainingSession) => void
+}
+
+const tabs: { id: Tab; label: string }[] = [
+  { id: 'info', label: 'Informações' },
+  { id: 'participants', label: 'Participantes' },
+  { id: 'qrcode', label: 'QR Code' },
+  { id: 'documents', label: 'Documentos' },
+]
+
+export function TrainingSessionDetailTabs({ session, onUpdateSession }: TrainingSessionDetailTabsProps) {
+  const [activeTab, setActiveTab] = useState<Tab>('info')
+
+  function handleArchive() {
+    onUpdateSession({ ...session, status: 'ARQUIVADO' })
+  }
+
+  function handleCancel() {
+    onUpdateSession({ ...session, status: 'CANCELADO' })
+  }
+
+  return (
+    <div className={styles.wrapper}>
+      <div className={styles.tabList} role="tablist">
+        {tabs.map((tab) => (
+          <button
+            key={tab.id}
+            role="tab"
+            aria-selected={activeTab === tab.id}
+            className={`${styles.tab} ${activeTab === tab.id ? styles.tabActive : ''}`}
+            onClick={() => setActiveTab(tab.id)}
+          >
+            {tab.label}
+          </button>
+        ))}
+      </div>
+      <div className={styles.panel}>
+        {activeTab === 'info' && (
+          <InfoTab session={session} onArchive={handleArchive} onCancel={handleCancel} />
+        )}
+        {activeTab === 'participants' && (
+          <ParticipantsTab session={session} onUpdateSession={onUpdateSession} />
+        )}
+        {activeTab === 'qrcode' && (
+          <QrCodeTab session={session} />
+        )}
+        {activeTab === 'documents' && (
+          <DocumentsTab session={session} />
+        )}
+      </div>
+    </div>
+  )
+}
